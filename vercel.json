{
  "buildCommand": "react-scripts build",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "env": {
    "CI": "false"
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
